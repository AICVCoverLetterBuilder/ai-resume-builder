/**
 * Permanent AAB-389 AI runtime regression revision.
 * Embeds the production-path suite identity into the runtime marker set so
 * device/diagnostic builds can prove the suite is linked.
 */
export const CV_AI_PERMANENT_AAB389_REGRESSION_REVISION =
  'cv-ai-permanent-aab389-regression-v1' as const;
