Fix localization consistency across the app so all generated content and exported document section titles always follow the user's currently selected language.

Important:
- Do NOT redesign the UI
- Do NOT change pricing, limits, or business logic
- Do NOT change the existing language selector behavior
- Keep English as the default first-launch language, but always respect the user's current selected language afterward

Tasks:

1. About page localization
- Make the entire About page fully localized
- It must always render in the currently selected app language
- It must update immediately when the user changes language
- Do not leave About page hardcoded in English

2. Smart Bullets localization
- In CV Builder → Work Experience → Smart Bullets
- The generated bullet content must be produced in the currently selected language
- It must not always return English
- If the selected language is Serbian, German, French, etc., the generated bullets must be in that exact language
- Keep the current Smart Bullets functionality, but make it language-aware

3. Professional Summary AI localization
- In CV Builder → Professional Summary → Generate with AI
- The AI-generated summary must always be returned in the currently selected language
- It must not default to English unless English is the selected language
- Make the AI prompt explicitly include the selected language and require output in that language
- Keep all current AI behavior and limits unchanged

4. DOCX export localization
- When exporting a DOCX file, all section headings must use the currently selected language
- This includes at minimum:
  - Professional Summary
  - Experience
  - Education
  - Skills
  - Certifications
- Also ensure any other section labels in the exported document follow the selected language
- The exported DOCX must match the visible app language at the moment of export

5. Single source of truth for language
- Ensure About page, Smart Bullets, AI Summary generation, and DOCX export all use the same current language source
- Do not let any of these features fall back to English unless English is actually selected
- If no language is selected yet, use English only as the default fallback

6. Verification
After fixing, verify all of the following:
- About page changes language correctly
- Smart Bullets output matches the selected language
- Professional Summary AI output matches the selected language
- DOCX exported section titles match the selected language
- No English text remains in these flows when another language is selected, except where intentionally untranslated by design

Return:
- exact files changed
- exact localization logic added
- confirmation that generated content and DOCX headings now follow the selected language