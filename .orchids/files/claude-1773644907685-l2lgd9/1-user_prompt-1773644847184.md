Verify the language initialization behavior after the recent update.

Do NOT make unrelated changes. Only inspect, test, and report. If a small fix is absolutely required to satisfy the checks below, apply only the minimum necessary change.

Please verify all of the following clearly:

1. First-time launch behavior
- In a fresh clean state (no saved language, no local storage, no cookies/session language preference), the app must open fully in English.
- This must be true across the entire app, not just the home page.

2. Global coverage
Confirm that English is the initial language on first launch for all major sections:
- Home page
- Resume / CV Builder
- Cover Letter Builder
- Templates
- FAQ
- Pricing / Upgrade / Pro sections
- Settings / language selector
- Any modals, placeholders, helper text, validation messages, buttons, banners, and onboarding text

3. No auto-detection on first launch
Confirm the app does NOT use browser locale, device locale, OS locale, or region to override the first-time language.
If any browser/device auto-detection still exists anywhere on first launch, identify it and remove it.

4. Persistence behavior
- Change the language manually to another supported language.
- Refresh the app.
- Confirm the selected language remains the same.
- Revisit the app again.
- Confirm the last selected language is still restored.

5. Safe fallback behavior
- If the saved language value is missing, invalid, corrupted, or unsupported, confirm the app safely falls back to English.

6. No flicker / no hydration mismatch
- Confirm there is no visible flash of a different language before English loads on first launch.
- Confirm there is no SSR/CSR hydration language mismatch.

7. Single source of truth
Confirm that language initialization is handled by one centralized global source of truth, not multiple conflicting per-page defaults.

8. Scope control
Do NOT change:
- UI design
- Layout
- Pricing
- Limits
- Features
- Business logic
- Existing translations
- Language selector UI

Final report required:
- State whether the language system is now fully compliant with:
  "First launch always English, future visits remember last selected language"
- If anything was adjusted, list only the exact minimal changes made.
- If any issue remains, describe it precisely.